#include "driver_mpu9250_read_test.h"

int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_USART1_UART_Init();
    MX_SPI1_Init();

    // *** ADD THIS BLOCK ***
    uint8_t res = mpu9250_read_test(MPU9250_INTERFACE_SPI, MPU9250_ADDRESS_AD0_LOW, 3); // 3 = perform 3 readings (enough for basic test)
    if (res == 0) {
        mpu9250_interface_debug_print("MPU9250 read test success!\r
");
    } else {
        mpu9250_interface_debug_print("MPU9250 read test failed!\r
");
    }
    // *** END BLOCK ***

    while (1) {
        // You can add more code here later.
    }
}
